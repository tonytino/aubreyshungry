{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo 'ORCHESTRATOR DIRECTIVE (repo standing policy): this session operates as an orchestrator by default. Read CLAUDE.md, then invoke the /orchestrate skill before multi-step work. Anything touching food content: read docs/agents/dietary-safety.md first — the golden rules. safe:agent PRs self-merge once CI is green; safe:human PRs stop at green for human review. Tiny tasks (questions, typo/doc fixes) may be done directly but still ship per PR conventions.'"
          }
        ]
      }
    ]
  }
}
