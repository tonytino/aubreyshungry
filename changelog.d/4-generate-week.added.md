- `[manual]` Weekly plan generation entry point (ADR-007): the committed `/generate-week` skill drives the owner-local drafting session end-to-end (inputs → deterministic gates → one adversarial dietary-safety review round → `safe:human` PR titled `feat(plan): <isoweek>`), backed by a Zod-validated household preferences config (`app/content/preferences.ts` + `content/preferences.json`), a `pnpm validate:content` repo-content gate, and a zero-LLM Thursday reminder workflow (`.github/workflows/plan-reminder.yml`) that opens an issue when the current week's plan file is missing.
