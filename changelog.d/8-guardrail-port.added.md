- `[propagate]` Added the `PR conventions` workflow (`.github/workflows/pr-conventions.yml`) ported from aubreyslist: a dependency-free conventional-commit PR-title check, the adversarial-review gate (`skip-review` label OR `review:adversarial-passed` label + a well-formed `## Adversarial review` PR-body section, validated by `.github/scripts/check-review-block.mjs`), and the owner-review gate (`.github/scripts/check-owner-review.mjs` fails any PR touching an owner-gated surface without the `safe:human` label; no bypass label).
- `[propagate]` Added `.github/scripts/owner-review-paths.mjs` as the single source of truth for owner-gated paths and content checks (destructive SQL in migrations, medical/nutritional-advice disclaimer copy, forbidden-ingredient tripwire in food-content files), with a bidirectional Vitest drift test against `.github/CODEOWNERS` (`tests/unit/check-owner-review.test.ts`).
- `[propagate]` Added session orchestration tooling: `CLAUDE.md` bootstrap, a `SessionStart` hook (`.claude/settings.json`), the `/orchestrate` and `review-loop` skills, and the deterministic `.claude/workflows/adversarial-review.mjs` review workflow — adapted to GitHub Issues and the dietary-safety golden rules.
- `[propagate]` Added the `review:adversarial-passed` and `skip-review` labels to `scripts/labels.mjs` (the Sync Labels workflow creates them on merge) and documented them in `docs/agents/tasks.md`.
- `[propagate]` Updated `docs/agents/governance.md` (the owner-review CI job is now live, with its honest limitations) and `docs/agents/orchestration.md` (CI review gate documented as live — a forcing function plus auditable record, not proof).
- `[propagate]` Added the `pr-tldr` CI job and `.github/scripts/check-tldr-block.mjs` (ported from aubreyslist): every PR body must lead with a filled-in plain-language `## TL;DR` section, now provided by `.github/pull_request_template.md`.
